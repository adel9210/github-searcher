import {Card} from '../Card/Card';
import {Repo} from "../../store/definations";

export const RepoCard = (repos: Repo) => {
    return (
        <Card>
            <h1>Repo Card</h1>
        </Card>
    );
};
