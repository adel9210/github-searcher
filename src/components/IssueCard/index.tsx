import {Card} from '../Card/Card';
import {Issue} from "../../store/definations";

export const IssueCard = (issue: Issue) => {
    return (
        <Card>
            <h1>Issue Card</h1>
        </Card>
    );
};
